[pytest]
testpaths = tests/step_defs
python_files = *_steps.py

markers =
    smoke: for smoke test
    regression: for regression
    dummymarker : dummy

#addopts = --browser=chromium --reruns 0 --html=reports/report.html --self-contained-html --capture=tee-sys --log-level=INFO -n 2
# Note: -n 2 (parallel execution) disabled because it conflicts with pytest-html screenshot embedding
# Note: --self-contained-html removed to support screenshot embedding with assets folder
addopts = --html=reports/report.html --capture=tee-sys --log-level=INFO